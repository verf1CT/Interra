# Чеклист публикации в сторы (Google Play / RuStore / App Store)

Состояние на 8 июля 2026. Что уже готово в репозитории и что осталось.

Сопутствующие материалы: `docs/store/store-listing.md` (тексты витрины),
`docs/store/data-safety.md` (ответы анкет), `docs/store/after-accounts.md`
(что делать после получения аккаунтов).

## Уже готово

- [x] Версия **1.0.0+2** (`app/pubspec.yaml`), в UI — `AppConfig.appVersion`.
- [x] **Release-подпись Android**: ключ `app/android/upload-keystore.p12` +
      `app/android/key.properties` (оба в .gitignore, в репозиторий не попадают).
      Локальная сборка: `flutter build appbundle` → `.aab` для Play,
      `flutter build apk` → `.apk` для RuStore.
- [x] **CI собирает и подписывает `.aab` + `.apk`** релизным ключом (keystore и
      пароли в GitHub secrets) на каждое изменение `app/**` и кладёт в **Releases**
      (тег `build-<N>`). Локальный Android SDK для выпуска не нужен —
      скачать готовый бандл и залить в стор.
- [x] **Иконка Google Play 512×512** — `docs/store/assets/google-play-icon-512.png`
      (из фирменного знака, без альфа). App Store 1024×1024 — в ассетах приложения.
- [x] **iOS Privacy Manifest** — `PrivacyInfo.xcprivacy` подключён к target Runner
      прямо в `project.pbxproj` (попадает в сборку).
- [x] **Резервная копия ключа сделана** — `~/Desktop/Интерра-ключ-подписи.zip`
      (keystore + пароль). При утере ключа обновлять приложение в сторах нельзя,
      хранить в надёжном месте (пароль-менеджер).
- [x] Имя приложения: **«ЛК Интерра»** (не конфликтует с занятым «Interra»).
- [x] `applicationId`: `ru.interra.lk_interra`, iOS bundle `ru.interra.lkInterra`.
- [x] Firebase подключён на Android и iOS, иконки/сплэш настроены.
- [x] **Клиентские push проверены на устройстве** — тестовое уведомление через
      Firebase Console дошло до телефона. Канал `interra_default` работает.
- [x] **JDK установлен** (`openjdk` через brew) + `adb` (platform-tools).
- [x] Тексты витрины и ответы Data Safety — подготовлены в `docs/store/`.
- [x] Политика конфиденциальности: `docs/PRIVACY_POLICY.md` — **нужно
      захостить** (страница на interra.ru или GitHub Pages) и указать URL в
      консолях сторов.

## Осталось сделать

### Блокеры
- [ ] **Push-бекенд `push.interra.ru`** — сейчас домена нет в DNS (NXDOMAIN),
      сервер не поднят. Приложение шлёт токены на этот адрес, без него рассылки
      не работают. Ждём серверных админов: DNS + деплой `server/` (зип и
      инструкция переданы) + `ADMIN_TOKEN`. После деплоя — сквозная проверка.
      _(Локальный Android SDK не нужен — подписанный `.aab`/`.apk` уже собирает CI,
      см. раздел «Уже готово».)_

### Общее
- [ ] Захостить политику конфиденциальности + пользовательское соглашение,
      получить публичные URL.
- [ ] Скриншоты витрины — на дизайнерах.
- [x] Иконка 512×512 (Play) / 1024×1024 (App Store) — готовы (см. «Уже готово»).
- [ ] Демо-аккаунт для модератора (ЛК за логином — без рабочего входа отклонят).
      Детали ниже — вход по SMS требует особого подхода.

#### Демо-аккаунт: как именно (вход по SMS-коду)

Подвох: вход — телефон + одноразовый SMS-код через биллинг UTM5 (`bbb`).
Модератор Apple/Google не может получить SMS на чужой номер, поэтому обычный
«логин/пароль» не подойдёт, а без рабочего входа заявку отклонят.

- [ ] **Тестовый абонент с фиксированным кодом** (правильный путь). Попросить
      админов биллинга завести служебного абонента, у которого:
  - есть выделенный номер телефона;
  - на запрос кода биллинг принимает **фиксированный OTP** (например, всегда
    `0000`) вместо реальной SMS;
  - на счёте есть баланс/данные, чтобы кабинет отрисовался (пустой ЛК ревьюер
    примет за «не работает»).
- [ ] Проверить, что этот номер реально логинится в приложении **до** подачи.
- [ ] Если бэкдор в биллинге невозможен — запасной вариант: видео прохода по
      приложению в заметках ревьюеру (работает хуже, Apple часто всё равно
      просит доступ).

Куда вписать при подаче:
- **App Store Connect** → *App Review Information* → **Sign-In required** →
  `User Name` = номер, `Password` = фиксированный код (или в *Notes*). В *Notes*
  описать флоу: «ввести номер X → "Получить код" → ввести код Y».
- **Google Play Console** → *App content* → **App access** → *restricted* →
  те же учётные данные и инструкция.

В заметках ревьюеру подчеркнуть нативную ценность (биометрия, push, диагностика
сети, сканер Wi-Fi, нативный баланс/виджеты) — чтобы пройти правило Apple 4.2
«не просто сайт». Подробнее — в `docs/APP_STORE_READINESS.md`.

### Google Play (ждём аккаунт от админов)
- [ ] Аккаунт разработчика ($25 разово).
- [ ] Если аккаунт личный — closed testing 20 тестеров 14 дней до продакшена.
- [ ] Скачать подписанный `.aab` из **Releases** (собирает CI) → загрузить.
- [ ] Анкета Data safety — ответы в `docs/store/data-safety.md`.
- [ ] Возрастной рейтинг, ссылка на политику конфиденциальности.

### RuStore (ждём аккаунт от админов на юрлицо)
- [ ] Аккаунт разработчика на ООО «Интерра» (нужна УКЭП).
- [ ] Скачать подписанный `.apk` из **Releases** (собирает CI) → загрузить.
- [ ] Push через FCM работают там, где есть сервисы Google; для полного покрытия
      (Huawei и часть РФ-прошивок) добавить RuStore Push — план внедрения в
      [`RUSTORE_PUSH.md`](RUSTORE_PUSH.md) (нужны доступы RuStore Console).

### App Store (после платного Apple Developer)
- [ ] Аккаунт $99/год.
- [ ] APNs-ключ (.p8) + Push capability, залить ключ в Firebase
      (см. `docs/IOS_PUSH_SETUP.md`) — без него iOS-пуши не идут.
- [ ] `aps-environment` → production, сборка через Xcode Organizer / Transporter.
- [ ] Privacy labels — ответы в `docs/store/data-safety.md`.

## Порядок

1. Push-бекенд + сквозная проверка уведомлений.
2. Privacy + пользовательское соглашение на сайт.
3. Google Play (дешевле/быстрее; параллельно closed testing, если аккаунт личный).
4. RuStore (РФ-аудитория).
5. App Store (дороже, дольше модерация; не забыть APNs-ключ).
