# Node
node_modules/
npm-debug.log*
.env
.env.*
!.env.example

# Databases / secrets
*.sqlite
*.sqlite-journal
*.db
server/data/
# Секретный ключ Admin SDK — НИКОГДА не коммитить
serviceAccountKey.json
# Примечание: google-services.json и GoogleService-Info.plist — клиентские
# конфиги Firebase (не секреты, и так попадают в выпущенный app). Коммитятся,
# чтобы сборка на другой машине (Mac для iOS) работала без доп. настройки.

# Flutter / Dart
app/.dart_tool/
app/.flutter-plugins
app/.flutter-plugins-dependencies
app/build/
app/.pub-cache/
app/.pub/
app/ios/Pods/
app/ios/.symlinks/
app/android/.gradle/
app/android/local.properties
app/android/key.properties
*.keystore
*.jks

# IDE / OS
.idea/
.vscode/
*.iml
.DS_Store
Thumbs.db
*.bak
